# NexusVideo Backend - 依赖清单
# Python >= 3.10

# === Web 框架 ===
fastapi==0.111.0
uvicorn[standard]==0.30.1          # ASGI 服务器，standard 含 websocket 支持

# === 数据校验 ===
pydantic==2.7.4
pydantic-settings==2.3.4           # 配置管理

# === HTTP 客户端（对接 ComfyUI REST API） ===
httpx==0.27.0                       # 异步 HTTP 客户端，支持 HTTP/2

# === WebSocket（对接 ComfyUI 实时进度） ===
websockets==12.0                   # ComfyUI ws:// 进度推送

# === 进程管理 ===
psutil==5.9.8                      # 进程查找、端口检测、系统资源监控

# === 工具 ===
python-multipart==0.0.9            # 文件上传支持（图生视频）
loguru==0.7.2                      # 结构化日志

# === P2 阶段 — 用户认证 ===
bcrypt==4.1.2                         # 密码哈希（bcrypt + 自动加盐）
